<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .card-body{
            text-align: center;
        }
    </style>
</head>
<body style="background: linear-gradient(to bottom, #0f0c29, #302b63, #24243e);">
    <%- include("./partials/navbar.ejs") %>

    
    <main class="justify-center row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 row-cols-xl-5 g-3 m-1">
        <% blogs.forEach(blog => { %>
            <div class="col">
                <div class="card shadow-sm border-0 text-center">
                    <img src="<%= blog.CoverImgURL %>" class="card-img-top img-fluid" alt="<%= blog.title %> cover image">
                    <div class="card-body p-3">
                        <h5 class="card-title text-right" style="max-width: 100%;"><%= (blog.title).toUpperCase() %></h5>
                        <p class="card-text small">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                        <a href="/blog/<%= blog._id %>" class="btn btn-primary btn-sm w-100">See more</a>
                    </div>
                </div>
            </div>
        <% }) %>
    </main>
    
    

    <%- include("./partials/script.ejs") %>
</body>
</html>