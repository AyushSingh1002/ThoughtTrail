<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<style>
    body{
      height: 100vh;
            background: linear-gradient(to bottom, #0f0c29, #302b63, #24243e);
            font-family: Arial, sans-serif;
            margin-top: 3.125rem;
            padding: 0;
            color: #fff;

        }

        .grid-container {
            margin: 1rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 2rem;
            padding: 4rem;
        }

        .card {
            cursor:grabbing;
            background-color: #fff;
            color: #000;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            display: flex;
            flex-direction: column;
            height: auto;
            width: auto;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
        }

        .card img {
            width: 100%;
            height: 250px;
            object-fit: contain;
        }

        .card-body {
            padding: 0.60rem;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            flex-grow: 1;
        }

        .card-title {
            font-size: 1.2rem;
            font-weight: bold;
            margin: 0.5rem 0;
        }

        .card-text {
            font-size: 0.9rem;
            color: #555;
            margin: 0.5rem 0;
        }

        .btn {
            background-color: #007bff;
            color: #fff;
            border: none;
            margin-top: .5rem;
            padding: 0.5rem;
            border-radius: 16px;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #0056b3;
        }
</style>
<body>
    <%- include("./partials/navbar.ejs") %>
  <%= locals.userInfo %>
  <main class="grid-container">
    <% blogs.forEach(blog => { %>
        <div class="card">
            <img src="<%= blog.CoverImgURL %>" alt="<%= blog.title %> cover image">
            <div class="card-body">
              <h5 class="card-title"><%= blog.title.toUpperCase() %></h5>
              <p style="text-align: center; opacity: .7;">  <%= blog.content.substring(0, 70) + "..." %></p>
                
                <a href="/blog/<%= blog._id %>" class="btn">See more</a>
            </div>
        </div>
    <% }) %>
</main>
</body>
</html>



  